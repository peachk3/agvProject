<mah:MetroWindow
        x:Class="agvProject.Views.Auth.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:agvProject.Views.Auth"
        mc:Ignorable="d"
        Title="Login"
        Height="600"
        Width="400"
        MinWidth="360"
        MaxWidth="500"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="37*"/>
            <RowDefinition Height="36*"/>
        </Grid.RowDefinitions>

        <TextBlock HorizontalAlignment="Center"
                VerticalAlignment="Top"
                TextAlignment="Center"
                TextWrapping="Wrap"
                FontFamily="Mongolian Baiti"
                FontSize="28"
                Margin="0,50,0,0">
                
        <Run Text="AGBee"
                FontWeight="Bold"
                FontSize="40"/>
                
                <LineBreak/>
                
        <Run Text=" "
                FontSize="6"/>
        <LineBreak/>

        <Run Text="Automated Guided Bee for Efficient Execution"
                FontSize="16"
                FontWeight="Normal"
                Foreground="Gray"/>
        </TextBlock>


        <StackPanel VerticalAlignment="Top"
                HorizontalAlignment="Center"
                Width="300" Margin="0,144,0,0" Grid.RowSpan="2">
            <!-- Logo -->
            <Image Source="/Views/Images/logo.png"
                Width="80"
                Height="100"
                Margin="0,0,0,20"/>

            <!-- 아이디 입력-->
            <TextBox x:Name="UserIdTextBox"
                mah:TextBoxHelper.Watermark="Username"
                mah:ControlsHelper.CornerRadius="10"
                Margin="0,0,0,10"
                Height="40"/>

            <!-- 비밀번호 입력 -->
            <PasswordBox x:Name="PasswordBox"
                mah:TextBoxHelper.Watermark="Password"
                mah:ControlsHelper.CornerRadius="10"
                Margin="0,0,0,10"
                Height="40"/>

            <!-- 로그인 버튼 -->
            <Button x:Name="LoginBtn"
                mah:ControlsHelper.CornerRadius="10"
                Content="LOGIN"
                Height="40"
                Margin="0,0,0,10"
                IsDefault="True"
                Click="LoginBtn_Click"/>

            <!-- 비밀번호 찾기 링크 -->
            <TextBlock Text="Forgot your password?"
                HorizontalAlignment="Center"
                Margin="0,10,0,0"
                Foreground="LightGray"
                Cursor="Hand"/>
        </StackPanel>
    </Grid>

</mah:MetroWindow>
